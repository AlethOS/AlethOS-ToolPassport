# Offline Contract Fixtures

This directory contains versioned, deterministic fixtures for shared contracts.
They must be verifiable without network access, redirects, credentials, or
third-party services.

`tool-identity/0.1.0/` fixes the reference Tool Identity normalization and
three-state resolution behavior. Each fixture contains existing Tool records,
one intake, and the exact expected resolution.
